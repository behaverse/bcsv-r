#' bcsv: Read, Write, Document, and Validate Better CSV Files
#'
#' Tools for working with CSV files paired with bcsv (Better CSV) metadata,
#' a JSON-LD extension of W3C CSVW. The public API mirrors the Python `bcsv`
#' package: four functions ([read_bcsv()], [write_bcsv()], [document_bcsv()],
#' [validate_bcsv()]) with snake_case parameter names and identical semantics
#' across languages.
#'
#' @section Schema version:
#' This package vendors a pinned copy of `bcsv/schema.json` at version
#' [SCHEMA_VERSION].
#'
#' @keywords internal
"_PACKAGE"
