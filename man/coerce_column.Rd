% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coercion.R
\name{coerce_column}
\alias{coerce_column}
\title{Coerce a raw character vector to its declared bcsv datatype.}
\usage{
coerce_column(raw, datatype, col_meta, pre_na_mask)
}
\arguments{
\item{raw}{Character vector (the raw column values, with NA tokens already
replaced by \code{NA_character_}).}

\item{datatype}{One of \code{string}/\code{integer}/\code{number}/\code{boolean}/\code{date}/
\code{datetime}/\code{time}/\code{categorical}/\code{ordered}.}

\item{col_meta}{The full column metadata list (used for \code{levels}).}

\item{pre_na_mask}{Logical vector—\code{TRUE} where the raw value was already
NA before coercion (an NA token, not a coercion failure).}
}
\value{
A list with \code{value} (coerced vector) and \code{failures} (logical vector
marking positions where the raw value was non-NA but coercion yielded
NA—i.e. COERCION_FAILED / LEVEL_NOT_DECLARED candidates).
}
\description{
Coerce a raw character vector to its declared bcsv datatype.
}
\keyword{internal}
