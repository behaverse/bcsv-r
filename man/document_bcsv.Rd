% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/document_bcsv.R
\name{document_bcsv}
\alias{document_bcsv}
\title{Generate a bcsv metadata draft from a typed tibble}
\usage{
document_bcsv(
  data,
  description,
  data_url = NULL,
  column_descriptions = NULL,
  pretty_name = NULL,
  creator = NULL,
  date_created = NULL,
  primary_key = NULL
)
}
\arguments{
\item{data}{A data frame / tibble whose columns have already been typed
(factors via \code{factor()} / \code{ordered()}, dates via \code{as.Date()}, etc.).}

\item{description}{Required description of what the table is about. The
bcsv schema (since v26.0608) requires every table to carry one.}

\item{data_url}{Optional URL/path that populates the metadata's \code{url}
field. When \code{NULL} (default), the field is omitted; \code{\link[=write_bcsv]{write_bcsv()}}
populates it from the \code{file} basename.}

\item{column_descriptions}{Named list of per-column overrides. Allowed
inner keys: \code{label}, \code{description}, \code{unit}, \code{minimum}, \code{maximum},
\code{min_length}, \code{max_length}, \code{na_strings}, \code{required}, \code{format}.
Unknown keys raise \code{bcsv_unknown_key_error}.}

\item{pretty_name}{Optional human-readable dataset title.}

\item{creator}{A length-1 character vector (simple form), a single list
with \code{name}/\code{email}/\code{orcid}/\code{affiliation} keys (structured form), or
a list of such structured creators (multi-author form).}

\item{date_created}{A \link{Date} for the metadata's \code{date_created} field.
Defaults to today.}

\item{primary_key}{A column name or character vector of column names
declaring the primary key. Stored under \code{table_schema$primary_key}.}
}
\value{
A list of metadata properties suitable for \code{\link[=write_bcsv]{write_bcsv()}}.
}
\description{
Infers bcsv datatypes from each column of \code{data}, then layers in the
overrides supplied via \code{column_descriptions}. Locked signature mirrors
the Python \code{bcsv.document_bcsv} (see the \href{https://github.com/behaverse/bcsv-r/blob/main/docs/04-api-spec.md}{API spec §3}).
}
