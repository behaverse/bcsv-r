% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/authoring.R
\name{draft_metadata}
\alias{draft_metadata}
\title{Draft an editable YAML metadata file for a dataset}
\usage{
draft_metadata(data, out = NULL, overwrite = FALSE, ...)
}
\arguments{
\item{data}{A CSV path or a data frame.
\itemize{
\item CSV path: the YAML is written next to it; the CSV is read for inference but
left untouched (\code{finalize_metadata} standardizes it later).
\item Data frame: \code{out} is an output \strong{stem} (e.g. "survey"); both \verb{<stem>.csv}
(canonical) and \verb{<stem>.yaml} are written. A CSV written from a typed data
frame is not byte-identical across R/Python—use the CSV-path entry if you
need that guarantee.
}}

\item{out}{Output stem (required when \code{data} is a data frame; ignored for a CSV path).}

\item{overwrite}{Overwrite an existing sidecar (or, for a df, an existing CSV). Default \code{FALSE}.}

\item{...}{Passed to \code{\link[=document_bcsv]{document_bcsv()}}.}
}
\value{
The YAML path (invisibly).
}
\description{
Draft an editable YAML metadata file for a dataset
}
