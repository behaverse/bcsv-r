% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/authoring.R
\name{finalize_metadata}
\alias{finalize_metadata}
\title{Standardize the CSV and write the canonical JSON sidecar}
\usage{
finalize_metadata(csv_path, keep_source = FALSE)
}
\arguments{
\item{csv_path}{Path to the CSV.}

\item{keep_source}{Keep the YAML source after writing JSON. Default \code{FALSE}.}
}
\value{
The \code{\link[=validate_bcsv]{validate_bcsv()}} result (invisibly).
}
\description{
Reads the CSV raw (value-preserving, honoring the declared dialect), rewrites it
canonically (comma, no row numbers, LF, UTF-8), hashes the standardized CSV, and
validates. On success it atomically replaces \verb{<stem>.csv} and writes \verb{<stem>.json}
(with a fresh \code{file_hash}), removing the YAML unless \code{keep_source}. On failure
nothing is changed. Standardizing a CSV file is byte-identical across R and Python.
}
