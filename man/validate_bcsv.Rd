% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_bcsv.R
\name{validate_bcsv}
\alias{validate_bcsv}
\title{Validate a CSV + bcsv-metadata pair}
\usage{
validate_bcsv(
  data_file,
  metadata_file = NULL,
  check_hash = TRUE,
  check_schema = TRUE,
  check_constraints = TRUE,
  on_violation = c("warn", "raise")
)
}
\arguments{
\item{data_file}{Path to CSV file.}

\item{metadata_file}{Path to metadata JSON file. If \code{NULL} (default), uses
\code{data_file} with \code{.csv} (case-insensitive) replaced by \code{.json}. \code{data_file}
must end in \code{.csv} for the default to apply; otherwise pass an explicit
\code{metadata_file}.}

\item{check_hash}{Compare SHA-256 of the CSV against \code{file_hash} in metadata.
Mismatch raises \code{bcsv_validation_error}; absent hash emits a \code{HASH_ABSENT}
warning. Default \code{TRUE}.}

\item{check_schema}{Validate metadata against \code{bcsv/schema.json} first.
Failures raise \code{bcsv_schema_error}. Default \code{TRUE}.}

\item{check_constraints}{Evaluate data-level constraints (\code{minimum}/\code{maximum},
\code{levels}, \code{min_length}/\code{max_length}, \code{required}, type coercion). Severity
is governed by \code{on_violation}. Default \code{TRUE}.}

\item{on_violation}{\code{"warn"} (default) keeps the row (with NA for coercion
failures or level violations) and emits a warning. \code{"raise"} raises
immediately. Does not affect schema or hash checks.}
}
\value{
A list with elements \code{valid}, \code{errors}, \code{warnings},
\code{data_columns}, \code{meta_columns}. Each \code{errors}/\code{warnings} entry is a list
with \code{code}, \code{message}, and \code{location}.
}
\description{
Verifies that a CSV file and its paired bcsv metadata are well-formed and
consistent. Collects issues into a \code{ValidationResult}; the function itself
does \strong{not} raise (callers inspect \code{result$valid} and \code{result$errors}).
Locked signature mirrors the Python \code{bcsv.validate_bcsv} (see the
\href{https://github.com/behaverse/bcsv-r/blob/main/docs/04-api-spec.md}{API spec §4}).
}
\details{
Pipeline (§4.2):
\enumerate{
\item Files exist (always).
\item \code{check_schema}—metadata conforms to \code{bcsv/schema.json}.
\item Column names and order match between CSV header and metadata.
\item \code{check_hash}—SHA-256 of CSV matches \code{file_hash}.
\item \code{check_constraints}—coerce columns, evaluate ranges / lengths / levels
/ required; check \code{primary_key} uniqueness.
}

When \code{on_violation = "raise"}, all collected warnings are promoted to errors
before returning (so \code{valid} flips appropriately).
}
