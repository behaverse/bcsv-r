% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_bcsv.R
\name{write_bcsv}
\alias{write_bcsv}
\title{Write a data frame + bcsv metadata as an atomic pair}
\usage{
write_bcsv(
  data,
  file,
  metadata_obj,
  metadata_file = NULL,
  overwrite = FALSE,
  validate = TRUE
)
}
\arguments{
\item{data}{A data frame / tibble to write.}

\item{file}{Path for the CSV output. Must end in \code{.csv} (case-insensitive)
for the default \code{metadata_file} resolution to succeed.}

\item{metadata_obj}{The bcsv metadata list to associate with \code{file}. If
\code{metadata_obj$url} is unset/empty, it is populated from \code{basename(file)}.}

\item{metadata_file}{Path for the metadata JSON. If \code{NULL} (default), uses
\code{file} with \code{.csv} replaced by \code{.json}.}

\item{overwrite}{Refuse to clobber existing files unless \code{TRUE}. Default
\code{FALSE}.}

\item{validate}{Run schema + constraint validation against \code{metadata_obj}
(and \code{data}) before committing to disk. Default \code{TRUE}. On failure
raises \code{bcsv_schema_error} or \code{bcsv_validation_error}; neither file is
touched.}
}
\value{
A list with elements \code{data} (CSV path written), \code{metadata} (JSON
path written), and \code{file_hash} (lowercase-hex SHA-256 of the CSV).
}
\description{
Writes the CSV and the metadata JSON via temp-then-rename atomicity so a
crash mid-write never leaves a stale/mismatched pair on disk. Locked
signature mirrors the Python \code{bcsv.write_bcsv} (see the \href{https://github.com/behaverse/bcsv-r/blob/main/docs/04-api-spec.md}{API spec §2}).
}
